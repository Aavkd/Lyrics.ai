# Flow-to-Lyrics: Phase 1 - Audio Analysis Backend
# =================================================

# Core Framework
fastapi>=0.109.0
uvicorn>=0.27.0
python-multipart>=0.0.6  # Required for file uploads

# Audio Processing
demucs>=4.0.0            # Vocal isolation (Hybrid Transformer)
librosa>=0.10.0          # BPM/onset detection
soundfile>=0.12.0        # Audio file I/O

# Deep Learning
torch>=2.0.0             # PyTorch (required by Demucs)
numpy>=1.24.0            # Numerical computing

# Phase 0 Dependencies (already installed)
g2p_en>=2.1.0            # Phonetic syllable counting

# Step 3: Generation Engine (Ollama Integration)
requests>=2.31.0         # HTTP client for Ollama API

# Phonetic Recognition (Phase 2.1 - Sound-Alike Generation)
allosaurus>=1.0.2        # Universal phone recognizer (IPA output)

# Phase C: Whisper Integration (improved phonetic accuracy)
openai-whisper>=20231117 # Whisper speech recognition for mumbled vocals
